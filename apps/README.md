# Apps

User-facing Setup OS applications live here.

- `desktop/`: local desktop shell for setup review, approvals, and generated systems.
- `web/`: browser-based app surface for hosted or local web workflows.
- `cli/`: command-line entrypoint for conversation ingestion, generation, and evolution.
